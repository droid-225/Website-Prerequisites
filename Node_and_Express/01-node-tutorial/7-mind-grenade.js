// Mind Grenade
// Module for 3-modules file

const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`The sum is: ${num1 + num2}`);
}

addValues()

// If we have a function to run inside a module, if we import the module using the require keyword, it will run when the class it was imported into is run